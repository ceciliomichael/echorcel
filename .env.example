# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/echorcel

# Auth Secret (used to encrypt sensitive data in database)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-change-this-in-production

# App URL (optional)
# Set this if using a reverse proxy or tunnel for the dashboard
# Example: https://echorcel.yourdomain.com
# APP_URL=

# Router / Domain configuration (OPTIONAL)
# Leave these unset for local-only mode (no tunnel/domain routing).
# Set these only if using Cloudflare Tunnel or similar for subdomain routing.
# Example: echosphere.cfd
# ROUTER_DOMAIN=echosphere.cfd

# Public hostname for the Echorcel dashboard (must match your DNS / tunnel config)
# Example: echorcel.echosphere.cfd
# ECHORCEL_HOSTNAME=echorcel.echosphere.cfd

# Docker Configuration (optional - auto-detects on all platforms)
# Only set if auto-detection fails:
# DOCKER_HOST=tcp://localhost:2375
