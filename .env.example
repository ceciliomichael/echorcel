# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/echorcel

# GitHub Token (optional - for accessing private repositories)
GITHUB_TOKEN=

# Docker Configuration (optional - auto-detects if empty)
# Leave empty for auto-detection, or set manually:
# Linux/macOS: unix:///var/run/docker.sock
# Windows (named pipe): npipe:////./pipe/docker_engine
# Windows (TCP): tcp://localhost:2375
DOCKER_HOST=

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
